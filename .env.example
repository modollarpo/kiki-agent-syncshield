# KIKI Agent™ Platform - Example .env

# Used by docker compose automatically when present as .env in repo root.
# Recommended workflow:
# 1) Copy this file to .env
# 2) Replace the CHANGE_ME values with strong secrets (use a password manager)
#
# Notes
# - INTERNAL_API_KEY is used for trusted server-to-server calls (e.g., Next.js route handlers -> API gateway).
# - JWT_SECRET + JWT_ALGORITHM are used when calling protected gateway routes via Bearer tokens.
# - CORS_ALLOW_ORIGINS controls gateway CORS; comma-separated list.

# --- Global/Shared ---
INTERNAL_API_KEY=CHANGE_ME_INTERNAL_KEY__32_CHARS_MIN
JWT_SECRET=CHANGE_ME_JWT_SECRET__32_CHARS_MIN
JWT_ALGORITHM=HS256
CORS_ALLOW_ORIGINS=http://localhost:3001

# --- Authentication Service ---
KIKI_JWT_SECRET=CHANGE_ME_KIKI_JWT_SECRET__64_CHARS_RECOMMENDED
KIKI_ACCESS_TOKEN_EXPIRE_MINUTES=30
KIKI_REFRESH_TOKEN_EXPIRE_DAYS=7
KIKI_ENV=development
KIKI_DATABASE_URL=postgresql://kiki:kiki_pass@postgres:5432/kiki_db

# Service-to-Service API Keys (for internal communication)
SYNCFLOW_API_KEY=CHANGE_ME_SYNCFLOW_API_KEY__32_CHARS_MIN
SYNCBRAIN_API_KEY=CHANGE_ME_SYNCBRAIN_API_KEY__32_CHARS_MIN
SYNCCREATE_API_KEY=CHANGE_ME_SYNCCREATE_API_KEY__32_CHARS_MIN
SYNCSHIELD_API_KEY=CHANGE_ME_SYNCSHIELD_API_KEY__32_CHARS_MIN

# --- Database ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=kiki
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# --- SyncBrain (LLM Orchestration) ---
OPENAI_API_KEY=your-openai-key

# --- SyncValue (LTV Prediction) ---
# (No external keys required unless using 3rd party ML APIs)

# --- SyncFlow (RTB/Ad Network) ---
# (Add ad network API keys as needed)

# --- SyncCreate (Creative Generation) ---
STABILITY_API_KEY=your-stability-key
RUNWAYML_API_KEY=your-runwayml-key
DEEPBRAIN_API_KEY=your-deepbrain-key
DID_API_KEY=your-did-key

# --- SyncEngage (CRM/Retention) ---
HUBSPOT_API_KEY=your-hubspot-key
KLAVIYO_API_KEY=your-klaviyo-key
SALESFORCE_API_KEY=your-salesforce-key
MAILCHIMP_API_KEY=your-mailchimp-key
ZOHO_API_KEY=your-zoho-key
ACTIVECAMPAIGN_API_KEY=your-activecampaign-key
SENDINBLUE_API_KEY=your-sendinblue-key
INTERCOM_API_KEY=your-intercom-key
PIPEDRIVE_API_KEY=your-pipedrive-key

# --- SyncShield (Compliance) ---
SYNC_DB_DSN=host=localhost user=postgres password=postgres dbname=syncshield port=5432 sslmode=disable
SYNC_S3_BUCKET=syncshield-audit-logs
AWS_REGION=us-east-1
# AWS credentials are picked up from environment or IAM role

# --- Redis ---
REDIS_HOST=localhost
REDIS_PORT=6379

# --- MinIO/S3 ---
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_ENDPOINT=http://localhost:9000
MINIO_BUCKET=kiki-assets

# --- Other ---
# Add any additional service-specific keys below
